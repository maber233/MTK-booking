# Email Configuration for MTK-booking on Cloud Run
# Copy these to your Cloud Run environment variables

# SMTP Configuration (recommended)
MAIL_TYPE=smtp-tls
MAIL_HOST=smtp.gmail.com
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_PORT=587
MAIL_AUTH=plain
MAIL_ADDRESS=info@mtk-booking.com

# Alternative: SendGrid SMTP
# MAIL_TYPE=smtp-tls
# MAIL_HOST=smtp.sendgrid.net
# MAIL_USER=apikey
# MAIL_PASSWORD=your-sendgrid-api-key
# MAIL_PORT=587
# MAIL_AUTH=plain
# MAIL_ADDRESS=info@mtk-booking.com

# For testing only (saves emails to files)
# MAIL_TYPE=file
# MAIL_ADDRESS=info@mtk-booking.com

# Instructions:
# 1. Choose one of the configurations above
# 2. Set these as environment variables in Cloud Run:
#    gcloud run services update mtk-booking --region=europe-west1 \
#      --set-env-vars="MAIL_TYPE=smtp-tls,MAIL_HOST=smtp.gmail.com,MAIL_USER=your-email@gmail.com,MAIL_PASSWORD=your-app-password,MAIL_PORT=587,MAIL_AUTH=plain,MAIL_ADDRESS=info@mtk-booking.com"
